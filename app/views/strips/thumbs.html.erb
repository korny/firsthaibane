<div class="ids">
<%- for strip_name in @strips -%>
  <%= id = Strip.id_from_strip_name(strip_name) %><%= link_to image_tag("/thumbnails/#{strip_name}"), :action => :show, :id => id %>
<%- end -%>
</div>
